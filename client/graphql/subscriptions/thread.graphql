subscription Thread($channelId: Int!) {
  threadAdded(channelId: $channelId) {
    id
    text
    channel {
      id
    }
    user {
      id
      name
      avatar
    }
    createdAt
    numberOfMessages
  }
}
