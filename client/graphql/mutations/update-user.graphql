mutation UpdateUser($input: UpdateUserInput!) {
  updateUser(updateUserInput: $input) {
    id
    userId
    name
    avatar
    introduction
    githubId
    twitterId
    skills {
      id
      name
    }
  }
}
