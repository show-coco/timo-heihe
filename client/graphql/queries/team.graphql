query Team($id: Int!) {
  team(id: $id) {
    id
    title
    description
    icon
    recruitNumbers
    isRequired
    repositoryUrl
    members {
      id
      userId
      name
      avatar
      memberState
    }
    owner {
      id
      userId
      name
      avatar
    }
    skills {
      id
      name
    }
    categories {
      id
      name
    }
  }
}
