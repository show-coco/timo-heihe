query Team($id: Int!) {
  team(id: $id) {
    id
    title
    description
    icon
    recruitNumbers
    isRequired
    repositoryUrl
    members {
      id
      name
      avatar
      memberState
    }
    owner {
      id
      name
      avatar
    }
    skills {
      id
      name
    }
    categories {
      id
      name
    }
  }
}
