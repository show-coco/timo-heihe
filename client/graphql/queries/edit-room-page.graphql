query RoomEditPage($slug: String!) {
  room(slug: $slug) {
    id
    title
    name
    slug
    description
    icon
    recruitNumbers
    isRequired
    repositoryUrl
    members {
      id
      name
      avatar
    }
    owner {
      id
      name
      avatar
    }
    skills {
      id
      name
    }
    categories {
      id
      name
    }
    types {
      id
      name
    }
  }
  categories {
    id
    name
  }
  skills {
    id
    name
  }
  roomTypes {
    id
    name
  }
}
